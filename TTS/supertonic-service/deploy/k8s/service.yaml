apiVersion: v1
kind: Service
metadata:
  name: supertonic-tts
  labels:
    app: supertonic-tts
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: supertonic-tts
