apiVersion: v1
kind: Service
metadata:
  name: kokoro-tts
  labels:
    app: kokoro-tts
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: kokoro-tts
---
# Optional: External access via LoadBalancer
# apiVersion: v1
# kind: Service
# metadata:
#   name: kokoro-tts-external
#   labels:
#     app: kokoro-tts
# spec:
#   type: LoadBalancer
#   ports:
#     - port: 80
#       targetPort: 8000
#       protocol: TCP
#       name: http
#   selector:
#     app: kokoro-tts
